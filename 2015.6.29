#sed 2015年6月29日14:43:52
## “*” 的特殊用法


``` java
#echo "abcd" | sed 's/z*/1/'
#1abcd
#echo "abcd" | sed 's/z*/1/g'
#1a1b1c1d1
#echo "abcd" | sed 's/b*/1/g'
#1a1c1d1
```

### 解释：
sed对于 \* 的处理 第一个例子中没有匹配到z，但是z\* 表示z可以出现零次，
目前还不能很好的理解，暂时理解为每个字符前都存在一个null字符，z\*可以匹配到
所以就会在字符串的最前面将该null字符替换为1。
例子2同理
例子3中，b被替换成了1，其余的字符前都添加了1
***
